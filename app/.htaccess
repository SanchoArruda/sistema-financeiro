# Bloqueio de acesso direto ao código interno da aplicação (Controllers, Models, Views)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
